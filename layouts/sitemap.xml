<urlset
    xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
    xmlns:xhtml="http://www.w3.org/1999/xhtml"
    >
    {{- range (where .Data.Pages "Section" "!=" "gallery") -}}
        <url>
            <loc>
                {{- .Permalink -}}
            </loc>
            {{- if not .Lastmod.IsZero -}}
                <lastmod>
                    {{- .Lastmod.Format "2006-01-02T15:04:05-07:00" -}}
                </lastmod>
            {{- end -}}
            {{- if .IsTranslated -}}
                {{- range .Translations -}}
                    <xhtml:link
                        rel="alternate"
                        hreflang="{{ .Lang }}"
                        href="{{ .Permalink }}"
                        />
                {{- end -}}
                <xhtml:link
                    rel="alternate"
                    hreflang="{{ .Lang }}"
                    href="{{ .Permalink }}"
                    />
            {{- end -}}
        </url>
    {{- end -}}
    {{- range slice "llms.txt" "llms-full.txt" "person.json" "projects.json" "topics.json" "ai-index.json" -}}
        <url>
            <loc>{{ . | absLangURL }}</loc>
        </url>
    {{- end -}}
</urlset>
